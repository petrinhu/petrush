# Handoff — sessão autônoma 2026-08-03 (líder dormiu)

## Estado ao fechar

- **Repo:** `github.com/petrinhu/petrush` · branch `main`
- **Últimas tags:** `v0.3.0` (UX wave), `v0.3.1` (pushd/!! — se push concluído)
- **CI:** Fedora 44 container · matrix clang/gcc × Debug/Release
- **v0.3.0 CI:** success (run após `fcb89b2`)
- **Local:** 9 suítes unitárias + smoke 22/22

## O que foi entregue nesta sessão (ordem)

1. Resume S0 + migração GitHub + wiki + limpeza forgejo  
2. Fix CI (format-truncation, newline-eof, smoke pwd) → Fedora 44  
3. Pesquisa features festejadas (Fish/Zsh)  
4. **v0.3.0:** alias, which, PS1, Tab-complete, history hints, `&&`/`||`  
5. **v0.3.1:** pushd/popd/dirs, `!!` / `!n`  

## Como provar ao acordar

```bash
cd ~/IDrive/Documentos/projetos_claudebrain/Projects/petrush
git pull
cmake -B build -S . -DCMAKE_BUILD_TYPE=Release
cmake --build build --target petrush check
./build/petrush
```

No REPL: `alias ll='ls -la'`, Tab, `pushd /tmp`, `popd`, `echo hi` depois `!!`.

## Pendências que SÓ o líder resolve

- Arquivar Codeberg (manual no site)  
- setuid de `pudod` em máquina real (DEPLOY_CHECKLIST + aprovação)  
- Confirmar escopo anti-OE (sem syntax-highlight full, sem plugins)

## Não precisa de alarme

Nada bloqueado à espera de senha/login/decisão irreversível no momento do handoff.

## Próximo (se continuar autônomo)

- `cd -` / `OLDPWD`  
- syntax highlight mínimo (só erro de aspas)  
- bump actions/checkout quando Node 20 for problema real  
